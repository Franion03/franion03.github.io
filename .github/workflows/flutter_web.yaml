name: Flutter Web

on:
  push:
    branches:
      - master
  schedule:
    - cron: "0 0 1 * *"
  workflow_dispatch:
  repository_dispatch:
    types: [project-updated]
      
jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.x'

    - name: Install Python dependencies
      run: pip install requests beautifulsoup4 pyyaml

    - name: Fetch LinkedIn Data
      env:
        LINKEDIN_CLIENT_ID: ${{ secrets.LINKEDIN_CLIENT_ID }}
        LINKEDIN_CLIENT_SECRET: ${{ secrets.LINKEDIN_CLIENT_SECRET }}
        LINKEDIN_REFRESH_TOKEN: ${{ secrets.LINKEDIN_REFRESH_TOKEN }}
      run: python scripts/fetch_linkedin.py
      
    - name: Log dispatch source (if triggered by another repo)
      if: github.event_name == 'repository_dispatch'
      run: |
        echo "Triggered by: ${{ github.event.client_payload.repo }}"
        echo "Project URL: ${{ github.event.client_payload.url }}"

    - name: Fetch Projects Data
      run: python scripts/fetch_projects.py
      
    - name: Commit Scraped Data
      uses: stefanzweifel/git-auto-commit-action@v5
      with:
        commit_message: "Automated update of portfolio data"
        file_pattern: "lib/model/*.dart lib/data/projects.dart"
      
    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        channel: 'beta'
        
    - name: Enable Flutter web 
      run: flutter config --enable-web
      
    - name: Install dependencies
      run: flutter packages get
      
    - name: Build web
      run: flutter build web
      
    - name: Deploy
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.PERSONAL_TOKEN  }}
        publish_dir: ./build/web